import { useState } from 'react';
import { PageHeader,Icon } from '../components/common/UI';
import { ServiceCard } from '../components/common/Cards';
import { HealthCTA } from '../components/home/Sections';
import { services } from '../data/services';
export default function Services(){const [category,setCategory]=useState('All services');const [search,setSearch]=useState('');const filtered=services.filter(s=>(category==='All services'||s.category===category)&&`${s.name} ${s.fullName||''} ${s.description}`.toLowerCase().includes(search.toLowerCase()));return <><PageHeader eyebrow="Our services" title="Care for every stage of life.">Explore consultations, diagnostics and support available through Hayat Health Care Clinic.</PageHeader><section className="section container"><div className="service-toolbar"><div className="filter-tabs" aria-label="Filter medical services">{['All services','Consultations','Diagnostics','Therapy & support'].map(c=><button key={c} aria-pressed={category===c} className={category===c?'selected':''} onClick={()=>setCategory(c)}>{c}</button>)}</div><label className="service-search"><Icon name="Search" size={18}/><span className="sr-only">Search services</span><input type="search" placeholder="Search services" value={search} onChange={e=>setSearch(e.target.value)}/></label></div><p className="result-count" aria-live="polite">{filtered.length} services</p><div className="services-grid">{filtered.map(s=><ServiceCard key={s.id} service={s}/>)}</div>{!filtered.length&&<div className="empty-state"><Icon name="Search" size={32}/><h2>No matching services</h2><p>Try a different search or contact the clinic for help.</p><button className="button" onClick={()=>{setSearch('');setCategory('All services');}}>Show all services</button></div>}<p className="section-note">Service and specialist availability may vary. Please contact the clinic before visiting.</p></section><HealthCTA/></>;}
