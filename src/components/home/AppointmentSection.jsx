import { clinicInfo } from '../../data/clinic';
import { Icon } from '../common/UI';
import RequestForm from '../common/RequestForm';
export default function AppointmentSection(){return <section className="section container appointment-section" id="appointment"><div className="appointment-intro"><span className="eyebrow">YOUR NEXT STEP TO BETTER CARE</span><h2>Let’s make time<br/>for your health.</h2><p>Tell us a little about your visit. We’ll help you connect with the right care.</p><ol className="appointment-steps">{[['Share your preferences','Choose a service, doctor and preferred time.'],['Send your WhatsApp request','Review your details and send them to our team.'],['Wait for confirmation','The clinic will confirm availability with you.']].map(([h,p],i)=><li key={h}><span>{String(i+1).padStart(2,'0')}</span><div><h3>{h}</h3><p>{p}</p></div></li>)}</ol><div className="appointment-help"><Icon name="Phone" size={22}/><div><p>Prefer to speak with us?</p><a href={`tel:${clinicInfo.phones[0]}`}>{clinicInfo.phones[0]}</a></div></div><p className="urgent-note">For urgent needs, call the clinic directly. WhatsApp enquiries may not be read immediately.</p></div><RequestForm/></section>;}
